<%= form_with(model: sub_skill, local: true) do |form| %>
  <% if sub_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sub_skill.errors.count, "error") %> prohibited this sub_skill from being saved:</h2>

      <ul>
      <% sub_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
