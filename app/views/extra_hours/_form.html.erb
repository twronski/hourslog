<%= form_with(model: extra_hour, local: true) do |form| %>
  <% if extra_hour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(extra_hour.errors.count, "error") %> prohibited this extra_hour from being saved:</h2>

      <ul>
      <% extra_hour.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id %>
  </div>

  <div class="field">
    <%= form.label :execution_date %>
    <%= form.date_select :execution_date %>
  </div>

  <div class="field">
    <%= form.label :profile_id %>
    <%= form.text_field :profile_id %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.number_field :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
