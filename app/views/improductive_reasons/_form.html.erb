<%= form_with(model: improductive_reason, local: true) do |form| %>
  <% if improductive_reason.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(improductive_reason.errors.count, "error") %> prohibited this improductive_reason from being saved:</h2>

      <ul>
      <% improductive_reason.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
