<%= form_with(model: voltage_level, local: true) do |form| %>
  <% if voltage_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(voltage_level.errors.count, "error") %> prohibited this voltage_level from being saved:</h2>

      <ul>
      <% voltage_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
